# Retrieval extras: semantic search (sentence-transformers) + numpy.
#
# This file is installed into the PRODUCTION image (backend/Dockerfile), so it is
# runtime surface, not a dev-only extra - CI audits it (security.yml). It is fully
# pinned so the audited versions and the installed versions are the same set.
#
# Resolution target: python:3.12-slim - CPython 3.12, manylinux x86_64.
# Regenerate deliberately when bumping, then re-run the Docker build to verify:
#
#   pip install --dry-run --report /tmp/r.json \
#     --platform manylinux2014_x86_64 --platform manylinux_2_17_x86_64 \
#     --platform manylinux_2_28_x86_64 --python-version 3.12 \
#     --only-binary=:all: --target /tmp/t -r requirements-retrieval.txt

# --- direct dependencies ---
sentence-transformers==6.1.0
numpy==2.5.3

# --- transitive (pinned) ---
annotated-doc==0.0.5
anyio==4.15.1
certifi==2026.7.22
click==8.5.0
cloudpickle==3.1.2
colorama==0.4.6
filelock==4.0.1
fsspec==2026.9.0
h11==0.16.0
hf-xet==1.6.0
httpcore==1.0.9
httpx==0.28.1
huggingface-hub==1.32.0
idna==3.20
Jinja2==3.1.6
joblib==1.6.0
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
mpmath==1.3.0
narwhals==2.26.0
networkx==3.7
packaging==26.3
Pygments==2.21.0
PyYAML==6.0.3
regex==2026.9.10
rich==15.0.0
safetensors==0.8.0
scikit-learn==1.9.1
scipy==1.18.1
setuptools==84.0.0
shellingham==1.5.4
sympy==1.14.0
threadpoolctl==3.7.0
tokenizers==0.23.2
torch==2.14.0
tqdm==4.70.1
transformers==5.17.0
typer==0.27.2
typing-extensions==4.16.0
